version: '3.3'

services:
  iotex-iopay-wallet-builder:
    build: 
      context: ../
      dockerfile: Dockerfile
    volumes:
      - $PWD/dist:/usr/app/src/electron/dist
    command: npm run build