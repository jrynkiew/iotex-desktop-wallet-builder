version: '3.3'

services:
  iotex-iopay-wallet-builder:
    build: 
      context: ../
      dockerfile: Dockerfile
    volumes:
      - ./out:/home/nvm/app/generated
    volumes:
      - $JRPC_iotex_wallet_builder/out:/usr/app/src/electron/dist
    command: npm run build:electron